import React from 'react';
import NavbarStyle from '../styles/navbar.module.scss'

const Navbar = (props) => {
   return(
       <h1 className={NavbarStyle.h1tag}>Welcome To Hackathons</h1>
   ) 
}

export default Navbar;